{
  "name": "ai-api-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Local AI API debugging proxy with a live dashboard",
  "license": "MIT",
  "engines": {
    "node": ">=18.18"
  },
  "scripts": {
    "start": "node server.js",
    "start:anthropic": "node server-anthropic.js",
    "start:openai": "node server-openai.js",
    "start:raw": "node server-raw.js",
    "check": "node --check server.js && node --check server-anthropic.js && node --check server-openai.js && node --check server-raw.js && node --check lib/config.js && node --check lib/store.js && node --check lib/sse.js && node --check lib/proxy.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^5.2.1",
    "ws": "^8.18.0"
  }
}
